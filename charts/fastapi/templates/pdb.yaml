{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "oip-fastapi.fullname" . }}-pdb
  labels:
    {{- include "oip-fastapi.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "oip-fastapi.selectorLabels" . | nindent 6 }}
{{- end }}