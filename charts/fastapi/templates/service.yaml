apiVersion: v1
kind: Service
metadata:
  name: {{ include "oip-fastapi.fullname" . }}
  labels: {{ include "oip-fastapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{ include "oip-fastapi.selectorLabels" . | nindent 4 }}
